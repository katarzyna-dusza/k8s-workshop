apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: backend
  labels:
    app: backend
spec:
  rules:
    - host: backend.domain.com
      http:
        paths:
          - path: /
            backend:
              serviceName: backend
              servicePort: 4000
